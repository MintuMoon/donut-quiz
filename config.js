// Global settings config
const CONFIG = {
    QUIZ_LENGTH: 20,                // Total active questions
    SECRET_CHANCE: 0.03,            // Chance for the secret question (3%)
    STORAGE_KEY: "donut_quiz_hist", // LocalStorage match history key
    STATS_KEY: "donut_quiz_stats"   // LocalStorage personal counter key
};
